<div dir="rtl">

# مینی‌پروژه‌ی RAG – نسخه‌ی سبک (Lite)

این نسخه برای دانشجویانی طراحی شده است که آشنایی کمی با Python و NLP دارند و فقط می‌خواهند یک سیستم RAG کاملاً ساده را پیاده‌سازی کنند.

## کارهایی که باید انجام دهید

### ۱. آماده‌سازی داده (نوت‌بوک 01)

1. دیتاست **Persian News Dataset** را از Kaggle دانلود کنید.
2. فایل CSV را در پوشه‌ی `data/raw/` قرار دهید.
3. با کمک `pandas` یک زیرمجموعه‌ی کوچک (مثلاً ۱۰ تا ۲۰ هزار خبر) بسازید.
4. متن‌ها را با چند قدم ساده تمیز کنید (حذف سطرهای ناقص، نرمال‌سازی ساده‌ی حروف و مانند آن).
5. خروجی نهایی را در مسیر `data/processed/news_subset.csv` ذخیره کنید.

### ۲. ساخت RAG ساده (نوت‌بوک 02)

1. خبرها را به چند بخش کوچک (چانک) تقسیم کنید.
2. برای هر چانک یک بردار embedding بسازید.
3. با FAISS یک ایندکس برداری بسازید.
4. تابعی بنویسید که:
   - یک سؤال فارسی را به‌عنوان ورودی بگیرد،
   - نزدیک‌ترین چانک‌ها را پیدا کند،
   - و پاسخ را با استفاده از همان چانک‌ها برگرداند (حتی اگر پاسخ فقط ترکیبی ساده از متن چانک‌ها باشد).

### ۳. ارزیابی و نتیجه‌گیری (نوت‌بوک 03)

1. حداقل ۱۵ سؤال درباره‌ی خبرهای زیرمجموعه بنویسید.
2. برای هر سؤال یک پاسخ مرجع کوتاه (جواب درست از نگاه خودتان) بنویسید.
3. سیستم RAG خود را روی این سؤال‌ها اجرا کنید.
4. برای هر سؤال مشخص کنید آیا جواب سیستم «قابل قبول» است یا نه.
5. در یک متن کوتاه (حدود یک صفحه) بنویسید:
   - سیستم شما چه‌قدر موفق است؟
   - چه خطاهایی بیشتر تکرار می‌شوند؟
   - چه ایده‌هایی برای بهبود نسخه‌ی بعدی دارید؟

## نکات مهم

- لازم نیست سیستم شما کامل یا حرفه‌ای باشد؛
- تمرکز این نسخه روی **درک مفهوم RAG** و **تمرین عملی ساده** است؛
- اگر بعضی بخش‌ها برایتان سخت است، می‌توانید نسخه‌ی ساده‌تری پیاده‌سازی کنید  
  (مثلاً پاسخ را فقط با نمایش چانک‌های برتر برگردانید).

موفق باشید 🌱

</div>
